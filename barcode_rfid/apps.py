from django.apps import AppConfig


class BarcodeRfidConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'barcode_rfid'
    verbose_name = 'Barcode & RFID Integration'
