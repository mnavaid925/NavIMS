from django.apps import AppConfig


class AlertsNotificationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'alerts_notifications'
    verbose_name = 'Alerts & Notifications'
